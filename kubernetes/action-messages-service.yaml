apiVersion: v1
kind: Service
metadata:
  name: action-messages-service
  namespace: techpathway-ns
spec:
  selector:
    app: action-messages
  ports:
    - port: 5001
      targetPort: 5001
  type: ClusterIP
